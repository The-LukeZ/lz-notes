{
  "name": "lz-notes",
  "private": true,
  "version": "0.0.1",
  "description": "Turns recorded meetings into structured notes with speaker diarization, exportable as PDF, DOCX, and Markdown.",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter lz-notes-web dev",
    "build": "pnpm --filter lz-notes-web build && pnpm --filter lz-notes-consumer build",
    "check": "pnpm -r check",
    "deploy": "pnpm --filter lz-notes-web deploy && pnpm --filter lz-notes-consumer deploy",
    "db:apply": "wrangler d1 execute lz-notes-db --file=./db/schema.sql --remote",
    "db:apply:local": "wrangler d1 execute lz-notes-db --file=./db/schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^4.97.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "workerd"]
  }
}
